// import { ADD_NEWONE_TASK } from "../constants";
const currentFilter = (state = [], actions: any) => {
  switch (actions.type) {
    default:
      return state;
  }
};

export default currentFilter;
